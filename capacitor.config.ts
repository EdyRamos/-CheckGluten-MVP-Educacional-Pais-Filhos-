import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.v2e.chefalerg.mvp',
  appName: 'Chef Alerg — MVP',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
